#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

typedef struct s_stack
{
    int *a_stack;
    int *b_stack;
    int a_size;
    int b_size;
} t_stack;


#endif